using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BombBall : MonoBehaviour
{
    /*
    void OnTriggerEnter2D(Collider2D other)
    {
        // Top, HealtManager objesinin collider'ına çarptığında ve patlama henüz tetiklenmemişse
        if (other.CompareTag("Enemy"))
        {
            HealtManagerRC healthManager = other.GetComponentInChildren<HealtManagerRC>();
            healthManager.explosionTriggered = false;
            if (healthManager != null)
            {
                // Patlama efektini tetikle
                healthManager.TriggerExplosion();
            }
        }
    }
    */
}
